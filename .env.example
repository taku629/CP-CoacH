# CP Coach 環境変数サンプル
# Vercel では Project Settings → Environment Variables に同じ key を登録する

# AI 診断文の生成に使用（未設定でもルールベースで動作するため任意）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# 本番公開URL（OGP / シェアリンクの絶対URL生成に使用）。例: https://cp-coach.vercel.app
NEXT_PUBLIC_SITE_URL=https://your-domain.example
